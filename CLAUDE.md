# Project rules

- Once a set of file writes for a task/request is finished, commit the changes (with a descriptive message) before ending the turn — don't leave finished work uncommitted for the user to commit manually.
